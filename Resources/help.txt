Update Npm packages

npm install -g npm-check-updates
ncu -u
npm install


composer global require afragen/wp-devops


wp package install wp-cli/dist-archive-command:@stable
wp dist-archive wp-content/plugins/plugin-starter plugin-starter-v1.0.0.zip --plugin-dirname=plugin-starter --filename-format='{slug}-{version}.zip'


Final: go to the plugin folder
# Clean vendor directory
rm -rf vendor

# Install prod-only dependencies
composer install --no-dev --optimize-autoloader

# Create the distribution zip
wp dist-archive .

---------------------
Make plugin Ready
---------------------
# Remove composer.lock file
rm -f composer.lock

# Remove vendor directory
rm -rf vendor

# Install dependencies WITHOUT dev packages
composer install --no-dev

# Regenerate optimized autoload files
composer dump-autoload -o

rm -f composer.lock && rm -rf vendor && composer install --no-dev && composer dump-autoload -o

composer install --no-dev --optimize-autoloader --classmap-authoritative
---------------------
Install svn
---------------------
install
sudo apt update
sudo apt install subversion -y
svn --version

---------------------
Modify plugin
---------------------
svn checkout https://plugins.svn.wordpress.org/plugin-starter
cd plugin-starter/trunk
svn add --force * 
svn commit -m "Update plugin to version X.Y.Z: added feature improvements and bug fixes"

cd ..
svn delete tags/1.0.1
svn commit -m "Remove incorrect 1.0.1 tag"

svn copy trunk tags/X.Y.Z
svn commit -m "Tagging version X.Y.Z"


jodi issue hoy
svn update
svn update tags
svn delete tags/1.0.1
svn commit -m "Remove incorrect 1.0.1 tag"
svn copy trunk tags/1.0.1
svn commit -m "Re-tag version 1.0.1 from trunk"




